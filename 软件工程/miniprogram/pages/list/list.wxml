<!--pages/list/list.wxml-->
<view class="container">
  <text>排行榜</text>
  </view>
  <block wx:for="{{score}}" wx:key="key">
    <van-card title="{{item.username}}" desc="分数：{{item.score}}" thumb="{{item.image}}" />
    <!-- <text>{{item.username}}:{{item.score}}</text> -->
  </block>
